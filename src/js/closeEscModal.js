//Добавляем слухача у функцію для закриття
//window.addEventListener('keydown', closeEscModal);
//modal.classList.add("is-open")

/* function closeEscModal (event) {
    if (event.code === "Escape") {
        onCloseModal()
    }
    return
}

// Закриття по кліку на бекдроп
function clickCloseModal(event) {
    if (event.currentTarget === event.target) {
        onCloseModal()
    }
    return
  }

  function onCloseModal() {
    window.removeEventListener("keydown",closeEscModal)
    modal.classList.remove("is-open")
  }

  export { onCloseModal,clickCloseModal} */
//Чекаю на модалку
